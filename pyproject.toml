[project]
name = "stock-time-transformer-b0"
version = "2.0.0"
description = "Causal cross-sectional Stock-Time Transformer for competition-score optimization"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.21",
  "pandas>=1.3",
  "scipy>=1.7",
  "swanlab>=0.9",
]

[project.urls]
Repository = "https://github.com/zeta172417-design/financial_modeling"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
